{
    // Sum of array function that takes array of number as argument
    const sumArray = (arr:number[]):number=>{
        // returning sum of array 
        return arr.reduce((acc,c)=> acc+c)
    }
    
}